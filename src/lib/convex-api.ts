import { api } from '../../convex/_generated/api';

/* eslint-disable @typescript-eslint/no-explicit-any */
// Codegen is environment-dependent. Keep a permissive facade so the app can build
// even when codegen cannot refresh function references in this sandbox.
export const convexApi: any = api;
